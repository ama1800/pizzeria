{% extends 'base.html.twig' %}

{% block title %}ProduitsOnMenu{% endblock %}

{% block body %}
    <h1>ProduitsOnMenu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produits_on_menu.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('produits_on_menu_index') }}">back to list</a>

    <a href="{{ path('produits_on_menu_edit', {'id': produits_on_menu.id}) }}">edit</a>

    {{ include('produits_on_menu/_delete_form.html.twig') }}
{% endblock %}
